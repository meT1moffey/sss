services:
  backend:
    build:
      context: ./flask
    ports:
      - "5000:5000"
    environment:
      PORT: 5000
  
  frontend:
    build:
      context: ./flask
    port:
      - "3000:3000"
    depends_on:
      - backend